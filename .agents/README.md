# AssessmentOS Agent Skills

Local workflow skills live in `.agents/skills/<skill-name>/SKILL.md`.

These skills are AssessmentOS-specific operating guides for future coding agents. They are original project instructions inspired by common agent workflow patterns, not vendored third-party code.

Use them when the task matches the skill description, and keep `AGENTS.md` as the short top-level routing guide.
